%klavyeden gırılen bır sayının belırtılen pozitif tam sayı ussunu alan
%program

clc;
clear;
clear all;
a=input('a sayisi=');
b=input('b sayisi=');

c=sonuc(a,b);
function c=sonuc(a,b)
c=a^abs(b);    %abs komutu mutlak değer işlevi görür
end
